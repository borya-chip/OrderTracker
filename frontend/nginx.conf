server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }

  location /api/ {
    proxy_pass ${BACKEND_URL};
  }

  location /v3/ {
    proxy_pass ${BACKEND_URL};
  }

  location /swagger-ui/ {
    proxy_pass ${BACKEND_URL};
  }
}
